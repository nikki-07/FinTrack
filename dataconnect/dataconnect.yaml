specVersion: "v1"
serviceId: "fintrack"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "fintrack-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
